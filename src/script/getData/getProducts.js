// import { postData } from "../api/api"

// export function getProducts(method, data = {}, params = "") {
//   const result = postData(method, (data = {}), (params = ""))
//   console.log(result)
// }
